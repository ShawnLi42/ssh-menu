# demo/

Media used in the main [README](../README.md):

- `demo.gif` — real WSL recording: arrow nav (preview follows) + live `f` favorite-toggle + filtering
- `01-fuzzy-menu.png` — the fuzzy menu (favorites, recent, tags, preview) — real `fzf` frame
- `02-filter-preview.png` — live filtering with the preview pane — real `fzf` frame
- `03-numeric-menu.png` — the no-`fzf` numbered fallback, grouped by site — real output
- `04-prod-guard.png` — the `PROD` confirmation guard — real output
- `05-tab-colors.png` — color reference: the hex tab colors the tool assigns per environment

All were captured from the fake [`ssh_menu.conf.example`](../ssh_menu.conf.example),
so no real hosts appear.
